// Generated by CoffeeScript 1.4.0
(function() {

  define([], function() {
    var Cake;
    return Cake = (function() {

      function Cake() {}

      Cake.get = function(name) {
        var cookie, cookies, parts, _i, _len, _ref;
        if (document.cookie) {
          cookies = document.cookie.split('; ');
          if (cookies) {
            _ref = cookies.slice(0);
            for (_i = 0, _len = _ref.length; _i < _len; _i++) {
              cookie = _ref[_i];
              parts = cookie.split('=');
              if (parts[0] === escape(name)) {
                return unescape(parts[1]);
              }
            }
          }
        }
        return null;
      };

      Cake.set = function(name, value, days) {
        var date, expiration;
        date = null;
        expiration = "";
        if (!(name != null) || name.search(/;/) !== -1) {
          throw 'name argument should be a string of length > 0';
        }
        if (days) {
          date = new Date();
          date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
          expiration = " expires=" + date.toUTCString();
        }
        return document.cookie = "" + (escape(name)) + "= " + (escape(value)) + "; " + expiration + "; path=/";
      };

      Cake.purge = function(name) {
        return Cake.set(name, "", -1);
      };

      return Cake;

    })();
  });

}).call(this);
